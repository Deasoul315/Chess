export type UserData = {
  userName: string;
  name: string;
  accessToken: string;
  // totalPoints: number;
  // matchesPlayedCount: number;
  // countEarnedMatches: number;
  // countLostMatches: number;
  // status: "ONLINE" | "OFFLINE";
};

export type UserProps = {
  userName: string;
  name: string;
  password: string;
  accessToken: string;
};
